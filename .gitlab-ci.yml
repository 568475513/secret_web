# 定义stage
stages:
  - deploy

abs-go:
  stage: deploy
  only:
    - develop
  script:
    - cd /data/wwwroot/abs_go
    - su www
    - git pull
  #    - nohup systemctl restart abs.service &
  #    - ./alived.sh stop
  #    - nohup ./alived.sh start &
  tags:
    - abs-go